import useDebounceMd from '../__docs__/README.md?raw';
import { Markdown, Meta } from '@storybook/blocks';
import useDebounce from './useDebounce.stories';

<Meta title="Hooks/useDebounce" of={useDebounce} />

<Markdown>{useDebounceMd}</Markdown>
